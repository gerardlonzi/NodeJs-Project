<script defer>
    window.onload = function() {
        const loader = document.querySelector('.container-widht-anim .img-loader');
        const textLoader = document.querySelector('.textLoader');
        const img = document.querySelector('.container-widht-anim img');
        console.log(img)
        console.log(loader)

    
        if (img) {
            img.onload = function() {
                loader.style.display = 'none';
                img.style.display = 'block';
            };
            img.src = img.getAttribute('src');
        }
    };
    
</script>